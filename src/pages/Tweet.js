import React from "react";

const Tweet = (props) => {
  const {
    id,
    owner,
    account_id,
    account_image,
    comments_count,
    likes_count,
    like_id,
    content,
    image,
  } = props;
  return <div>PlaceHolder Texts</div>;
};

export default Tweet;
